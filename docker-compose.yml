services:
  meal-planner:
    build: .
    image: meal-planner:latest
    container_name: meal-planner
    restart: unless-stopped
    ports:
      - "3456:3000"
    environment:
      TZ: Europe/Berlin
    volumes:
      - ./data:/data
